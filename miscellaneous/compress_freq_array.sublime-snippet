<snippet>
	<content><![CDATA[
sort(all(a));

vector<pair<int, int>> ${1};
for (int i = 0; i < n; i++) {
    if (${1}.empty() or ${1}.back().first != a[i]) ${1}.push_back({a[i], 1});
    else ${1}.back().second++;
}

${2}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>compress_freq_array</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
